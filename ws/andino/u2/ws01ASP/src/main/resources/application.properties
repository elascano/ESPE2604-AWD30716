# Server configuration
server.port=8080

# MongoDB Atlas configuration
# Replace <usuario>, <password>, <cluster-url> and <database> with your real credentials
spring.data.mongodb.uri=mongodb+srv://admin:admin@awd.ypmipjt.mongodb.net/Products?retryWrites=true&w=majority

# Thymeleaf configuration (disable cache in development to see changes)
spring.thymeleaf.cache=false
