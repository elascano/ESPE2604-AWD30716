services:
  - type: web
    name: erazo-student-grades
    env: docker
    plan: free
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DATABASE
        value: student_grades
